<% if @student.errors.any? %>
  $("#studentError").html("<%= escape_javascript render("admin/shared/error", model: @student) %>");
<% else %>
  $("#studentsTable").prepend("<%= escape_javascript render('student_row', student: @student) %>");
  $(".close-modal").click();
<% end %>